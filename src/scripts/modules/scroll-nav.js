// let navItems = Array.from(document.querySelectorAll(".js-nav"));

// let options = {
// 	root: null,
// 	rootMargin: "0px",
// 	threshold: 0.1
// };

// let observer = new IntersectionObserver(animateMenuItem, options);

// let targets = document.querySelectorAll(".");

// function animateMenuItem(entries, observer) {
// 	entries.forEach(entry => {
// 		if (entry.isIntersecting) {
// 			console.log(navItems);
// 			navItems.forEach(item => {
// 				item.style.transform = "translate(-100px, -100px)";
// 			});
// 		}
// 	});
// }

// targets.forEach(target => {
// 	observer.observe(target);
// });
